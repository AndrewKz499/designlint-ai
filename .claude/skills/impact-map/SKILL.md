---
name: impact-map
description: Шаблон карты архитектурной разведки (impact analysis) для Lead Architect проекта DesignLint AI. Используй ВСЕГДА перед нелинейным изменением кода — затрагивающим 3+ файла, меняющим контракт PluginMessage, добавляющим новый тип сообщения, или содержащим развилку A/B. НЕ используй для линейных косметических правок (один цвет, одна строка, одна опечатка) — для таких сразу маршрутизация без карты.
---

# Skill: impact-map

Этот навык формирует **карту влияния** для нелинейного изменения в кодовой базе DesignLint AI. Карта помогает тебе самому выбрать вариант реализации **до** того, как исполнитель начнёт писать код, и снижает долю «дозаправочных» коммитов после неточной разведки. По спорным развилкам (влияющим на объём v1.0/v1.1 или публичный контракт) карта становится основанием для эскалации к пользователю.

## Когда использовать

Используй этот навык, когда задача попадает хотя бы в один из случаев:

- Изменение затрагивает **3+ файла** в разных слоях (sandbox / UI / shared).
- Меняется **контракт `PluginMessage`** (новый тип, переименование, добавление поля).
- Появляется **развилка A/B** — два разных архитектурных подхода с разными trade-offs.
- Затрагиваются `figma.clientStorage`-ключи (риск миграции для существующих пользователей).
- Появляется **async-операция**, способная зависнуть UI без индикатора прогресса.
- Есть **опасная мутация** Figma-документа (удаление нод, batch на `figma.root.children`).

## Когда НЕ использовать

Линейная косметика — **без impact-map**, сразу маршрутизация к исполнителю:

- Один цвет в `tokens.ts` → `@ui-engineer`.
- Одна строка в `strings.ts` → `@ui-engineer` (если UI) или `@backend` (если sandbox).
- Опечатка в комментарии → исполнитель по затронутой папке.
- Замена одного хардкода на токен в одном компоненте → `@ui-engineer`.

Для таких задач выдавай **Форму 1** из своей карты роли, а не полную карту.

## Что НЕ делай этим скиллом

- Не пиши код — это работа `@backend` / `@ui-engineer`.
- Не подменяй разведку догадками. Если не прочитал `src/shared/types.ts` или не грепнул обработчики — нельзя выдать карту.
- Не дублируй разведку, которая уже есть в `context/architecture/` или `context/sessions/`. Сначала найди существующее, потом дополняй.
- Не выбирай вариант A или B **сам**, если развилка попадает в один из триггеров эскалации (см. раздел «Когда выбираешь сам, когда эскалируешь пользователю»). По остальным развилкам — выбираешь сам и фиксируешь обоснование в разделе «Рекомендация».

## Когда выбираешь сам, когда эскалируешь пользователю

Это новый раздел, появившийся после консолидации команды. Раньше функции «формулирую развилку» и «выбираю вариант» были у двух разных агентов (`@architect` → `@project-lead`). Теперь они у тебя одного — но не любая развилка должна решаться без человека.

**Выбираешь сам** (карта = решение, исполнитель идёт в работу сразу):
- Технические развилки внутри одного слоя (useState vs useRef, Map vs Object, eager vs lazy load).
- Микро-архитектурные паттерны, не меняющие публичный контракт.
- Выбор формата протокольного сообщения, когда обе альтернативы симметричны и не влияют на UX.

**Эскалируешь пользователю** через `ask_user_input_v0` (карта = основание для решения, ждёшь ответа человека):
- Развилка влияет на **объём v1.0 / v1.1** (что-то добавить или убрать из релиза).
- Развилка меняет **публичный контракт плагина** (`manifest.json` — id, разрешения, имя).
- Развилка с **длительными последствиями** (выбор архитектуры темизации, формат хранения метрик, базовый формат данных).
- Развилка **конфликтует с зафиксированным принципом** (см. CLAUDE.md «Архитектурные принципы»).
- Развилка добавляет в палитру **новый цвет / новый компонент дизайн-системы**, отсутствующий ранее.

## Обязательные секции карты

Каждая карта содержит **все** эти секции в строгом порядке. Если какая-то секция «не применима» — пиши явно «не применимо: <причина>», не пропускай.

| Секция | Что туда писать |
|---|---|
| **Заголовок** | `## Карта изменения: <короткое название>` |
| **Затронутые файлы** | Все файлы прямого и косвенного влияния по слоям (sandbox / UI / shared). Для каждого — что именно меняется. |
| **Контракт `PluginMessage`** | Новые типы, изменённые типы, проверка симметрии в обе ветки try/catch. |
| **`clientStorage`-ключи** | Затронутые ключи, нужна ли миграция, есть ли fallback на старое имя. |
| **Риски** | Что может пойти не так. Регрессии в acceptance criteria закрытых фаз. |
| **Варианты A/B** | Минимум два варианта с trade-offs. Если вариант один — явно «развилка отсутствует, единственный вариант». |
| **Решение или эскалация** | Какой вариант выбран и почему — **или** явная эскалация к пользователю с указанием триггера. |
| **Следующий шаг** | Конкретный шаг для исполнителя в формате проекта (Цель / Зачем / Действие / Критерий / Среда / Исполнитель). Заполняется после получения решения, если была эскалация. |

## Формат вывода

Используй ровно этот шаблон:

```markdown
## Карта изменения: <короткое название>

### Затронутые файлы

**Sandbox (`src/sandbox/`):**
- `code.ts` — <что меняется>
- `detector.ts` — <что меняется или «не затрагивается»>

**UI (`src/ui/`):**
- `App.tsx` — <что меняется>
- `components/<Имя>.tsx` — <что меняется>

**Shared (`src/shared/`):**
- `types.ts` — <что добавляется в `PluginMessage`>
- `strings.ts` — <если затрагиваются строки>

### Контракт PluginMessage

**Новые типы:**
- `<TypeName>` — <структура и направление: UI → sandbox или sandbox → UI>

**Изменённые типы:**
- `<TypeName>` — <что добавилось/изменилось>

**Симметрия проверена:**
- `<request-type>` ↔ `<response-type>` в try-ветке: ✅
- `<request-type>` ↔ `<response-type>` в catch-ветке: ✅ / ⚠️ требуется

### clientStorage-ключи

- Затронутые ключи: `<key1>`, `<key2>` / **не применимо**: <причина>
- Миграция со старого имени: нужна / не нужна
- Fallback-чтение в первые N версий: <план>

### Риски

1. <риск 1>
2. <риск 2>

**Регрессии в закрытых фазах:**
- Ф.<NN.X> «<acceptance>» — <под угрозой / не затронута>

### Варианты A/B

**Вариант A: <название>**
- Описание: <1–2 предложения>
- Плюсы: <bullet>
- Минусы: <bullet>
- Объём правок: ~N файлов / ~M строк
- Влияние на бандл: ~<значение KB>

**Вариант B: <название>**
- Описание: <1–2 предложения>
- Плюсы: <bullet>
- Минусы: <bullet>
- Объём правок: ~N файлов / ~M строк
- Влияние на бандл: ~<значение KB>

### Решение или эскалация

Один из двух блоков ниже — не оба.

**Если выбираешь сам:**

> **Выбран вариант <A или B>.**
> Обоснование: <1–3 предложения по приоритетам v1.0/v1.1, по сравнению trade-offs, по соответствию принципам>.

**Если эскалируешь пользователю:**

> **Эскалация к пользователю.**
> Триггер эскалации: <конкретный пункт из раздела «Когда выбираешь сам, когда эскалируешь пользователю»>.
> Требуется решение: вариант A или B.
> Я рекомендую: <A или B> — потому что <обоснование>, но решение за тобой.

### Следующий шаг (формат проекта)

Заполняется **после** того, как решение принято (тобой или пользователем).

- **Цель:** <одна строка>
- **Зачем:** <1–2 предложения>
- **Действие:** <конкретные правки>
- **Критерий успеха:** <1–2 строки проверяемого результата>
- **Среда:** Claude Code в терминале
- **Исполнитель:** @<роль> (+ @qa для проверки)
```

## Если развилки A/B нет

Иногда после разведки оказывается, что вариант **один** (например, симметрия `PluginMessage` нарушена и есть только один способ её восстановить). В этом случае:

```markdown
### Варианты A/B

**Развилка отсутствует.** Единственный вариант:
- <описание>
- Обоснование, почему альтернатив нет: <1–2 предложения>

### Решение или эскалация

Развилки нет — эскалация не требуется. Переходим к следующему шагу.
```

Это **корректный** результат разведки, не пробел.

## Связь с архитектурными принципами

Этот skill реализует принципы из CLAUDE.md:

- **Симметрия `PluginMessage`** — ответ есть в обеих ветках try/catch. Прецедент: `fix-violation` без `fix-complete` в catch до 12.7.4.1.
- **`figma.commitUndo` — ДО мутации.**
- **Атомарность изменений** — карта должна быть привязываемой к одному коммиту (или явно говорить «потребуется N коммитов»).

## Перед отдачей карты проверь

- [ ] Прочитан актуальный `src/shared/types.ts` (контракт, на который опирается всё).
- [ ] Прогрепаны обработчики `msg.type ===` в обоих слоях.
- [ ] Прогрепаны `figma.clientStorage` (если применимо).
- [ ] Прогрепаны `parent.postMessage` и `figma.ui.onmessage` (если меняется протокол).
- [ ] Каждая обязательная секция заполнена (или явно «не применимо»).
- [ ] Симметрия `PluginMessage` проверена в **обе** ветки try/catch.
- [ ] Сформулировано минимум два варианта A/B (или явно «развилка отсутствует»).
- [ ] Каждый вариант имеет trade-offs, не только описание.
- [ ] В разделе «Решение или эскалация» — **либо** выбран вариант с обоснованием, **либо** явная эскалация к пользователю с указанием триггера. Не оба сразу, не пусто.
- [ ] Если эскалация — следующий шаг пока не заполнен (заполняется после ответа пользователя).
- [ ] Если решение принято тобой — следующий шаг сформулирован в формате проекта (Цель / Зачем / Действие / Критерий / Среда / Исполнитель).
